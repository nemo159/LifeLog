<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <!--
      [설명]
      - Android 12+에서 백업/복원 시 “데이터 추출”에 대한 규칙을 정의합니다.
      - 클라우드 백업/기기 간 전송 시 어떤 데이터를 포함할지 통제합니다.
      - 라이프로그는 민감 정보 가능성이 있으므로 기본은 제외 권장입니다.
    -->

    <!-- 클라우드 백업 규칙 -->
    <cloud-backup>
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
        <exclude domain="sharedpref" path="." />
    </cloud-backup>

    <!-- 기기-기기 전송 규칙 -->
    <device-transfer>
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
        <exclude domain="sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>
